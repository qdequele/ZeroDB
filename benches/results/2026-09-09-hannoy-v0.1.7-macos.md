# hannoy v0.1.7-nested-rtxns on LMDB vs ZeroDB — divan benches, macOS laptop, 2026-09-09

Produced by `scripts/hannoy.sh bench` (see docs/CONSUMER-GATE.md). Indicative only: the production target is Graviton + EBS gp3.

- hannoy: v0.1.7-nested-rtxns, `heed = "0.22.1-nested-rtxns"` → heed-shim 0.22.1 (ZeroDB feb03c2 + this branch); zero source changes; `lmdb-master-sys` absent from the ZeroDB build's dependency tree.
- Machine: Apple M1 Pro, macOS 27.0. `cargo bench --bench benchmark` (divan, codspeed-divan-compat), one round, LMDB then ZeroDB.
- `build_hnsw`: HNSW build over 5 000 random vectors, ef_construction 32, M 16 / M0 32 (vector insertion untimed). `search_hnsw`: 10-NN by vector over a 50 000-vector index, 100 samples.

```text
hannoy `cargo bench --bench benchmark`, median per bench (1 round(s) per engine)
   bench                                           lmdb         zerodb  ratio
   build_hnsw::1536                            1.659 s      1.681 s    1.01x
   build_hnsw::512                            937.30 ms     1.052 s    1.12x
   build_hnsw::768                             1.175 s      1.342 s    1.14x
   search_hnsw::1536                            3.13 ms      2.88 ms   0.92x
   search_hnsw::512                             2.24 ms      2.40 ms   1.07x
   search_hnsw::768                             2.47 ms      2.22 ms   0.90x

ratio < 1.00x means ZeroDB is faster.
```

Reading: search is at parity or better (0.90–1.07x; July 2026 read 0.95x). Build is 1.01x at 1536 dims and 1.12–1.14x at 512/768 — down from 1.27–1.49x in July; HNSW build is read-bound (a `get` per candidate distance evaluation), so the remaining gap is per-`get` overhead diluted by distance math as the dimension grows.

Suite note: hannoy's test suite at this tag has 10 insta snapshot failures **on both engines** (the tag bumped the crate version to 0.1.7 without refreshing snapshots that print `Version { … patch: 6 }`; every diff is that one line). 34 passed / 10 failed / 1 ignored identically on LMDB and on ZeroDB. v0.1.3, the version milli pins, was fully green on ZeroDB in July.
