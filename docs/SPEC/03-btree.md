# SPEC 03 — B+tree algorithms (Phase 0.4 deliverable)

Status: TO BE WRITTEN. Search, cursor state machine (all LMDB cursor ops and
their exact positioning/error semantics), insert with split, delete with
rebalance/merge, COW rules (first-touch copy, parent chain), overflow chains,
DUPSORT trees. Include the tree invariants the `check` tool enforces.
