[package]
name = "zerodb"
version = "0.1.0"
description = "A pure-Rust, transactional, memory-mapped key-value engine with LMDB parity behind the heed API and no C dependency."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
readme = "../../README.md"
keywords = ["lmdb", "heed", "key-value", "embedded", "database"]
categories = ["database-implementations", "database"]

[dependencies]
zerodb-core = { path = "../zerodb-core", version = "=0.1.0" }
zerodb-io = { path = "../zerodb-io", version = "=0.1.0" }
