[package]
name = "zerodb-io"
version = "0.0.1"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
zerodb-core = { path = "../zerodb-core" }
memmap2.workspace = true
# pwritev for the vectored commit write path (PERF-GAP B4); allowlisted.
libc.workspace = true
