# ZeroDB

Pure-Rust transactional KV engine: LMDB parity behind the heed API, then
optimized for Meilisearch and hannoy. See PLAN.md (roadmap), CLAUDE.md
(agent rules), docs/SPEC (format & algorithm spec — source of truth).

Start here: `claude` then `/milestone 0.1`.
